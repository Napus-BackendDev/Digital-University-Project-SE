version: "3.9"

services:
  vue-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: vue-app
    ports:
      - "80:80"
    restart: unless-stopped
    networks:
      - default
      - backend_default

networks:
  backend_default:
    external: true
